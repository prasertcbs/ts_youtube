let flowers: string[]=['lily', 'jasmnine', 'carnation']
console.log(flowers);

// for each *in*
for (let v in flowers) {
    console.log(v);
}

// for each *of*
for (let v of flowers) {
    console.log(v);
}
